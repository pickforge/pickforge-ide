{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window. App-defined pty_* commands are accessible without ACL entries; this grants the Tauri core defaults.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
